let width = 100,
height = 50,
background = 'yellow',
value = 'Touch',
borderRadius = 10;

// const newButton = new Button(width, height, background, value);
// newButton.render();
const addRadius = new ModernButton(width, height, background, value, borderRadius);
addRadius.render();
    